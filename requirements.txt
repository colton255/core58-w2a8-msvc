# Shared dependencies for the repo's CPU and GPU Python entrypoints.
# Install this file into both `venv_cpu` and `venv_gpu`.
# PyTorch is installed separately in the GPU environment so users can pick
# the correct CUDA wheel for their local Windows setup.

cmake>=4.0.0
fire>=0.5.0
sentencepiece>=0.2.0
huggingface-hub>=0.19.0
numpy>=1.24.0
transformers>=4.36.0
safetensors>=0.4.0
einops>=0.7.0
fastapi>=0.110.0
uvicorn>=0.27.0
pyyaml>=6.0
tqdm>=4.66.0
